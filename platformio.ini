; Convenience note: each firmware example is its own PlatformIO project.
;
;   pio run -d firmware/examples/realtime
;   pio run -d firmware/examples/scan
;   make build
;
; Per-example configuration lives in firmware/examples/*/platformio.ini so the
; .ino sketch can sit at the project source root (PlatformIO requirement).
